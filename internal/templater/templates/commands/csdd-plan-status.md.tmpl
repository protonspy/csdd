---
description: Show a csdd plan's derived status — per-feat state (pending/…/done), approval, and drift.
argument-hint: <plan-slug>
allowed-tools: Bash(csdd plan status:*), Bash(npx @protonspy/csdd plan status:*)
---

## Task

Run `csdd plan status $ARGUMENTS` and summarize: which feats are done, in
progress, or pending; and whether the plan is approved and drift-free. State is
derived from `specs/` reality and the run ledger — there is nothing to edit here.
